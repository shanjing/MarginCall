[pytest]
testpaths = tests
asyncio_mode = auto
collect_ignore = [".venv"]
markers =
    integration: marks tests that call external APIs or run full agent evals (deselect with -m "not integration")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
